import AdminLoginForm from "@/components/AdminLoginForm";

export default function LoginPage() {
  return (
    <main className="min-h-screen flex items-center justify-center bg-gradient-to-r from-pink-50 via-white to-purple-50 p-4">
      <AdminLoginForm />
    </main>
  );
}